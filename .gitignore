# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.pnpm-store/

# Build output
dist/
build/
out/
*.tsbuildinfo

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key
.vault-credentials

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# Test / coverage
coverage/
.nyc_output/
.vitest-cache/

# Temp
tmp/
temp/
.tmp/

# Runtime / local data (dogfooding domain-memory inside this repo)
.domain-memory/*
!.domain-memory/knowledge/

# Models cache (Transformers.js downloads them lazily)
.cache/
models/
*.onnx

# Presentation (talk material kept outside the repo)
presentation/

# Transient publish artefacts — `prepack` copies these into each package
# right before `npm pack` / `npm publish`; `postpack` removes them after.
packages/cli/templates/
packages/*/README.md
packages/*/LICENSE

# Eval runner output (baseline.json is committed; last-run.json is per-machine)
evals/last-run.json
